# memddc-ai-skill

## 项目信息

- **项目主页**: https://github.com/qihao123/memddc-ai-skill
- **作者**: qihao123
- **联系方式**: qihoo2017@gmail.com
- **版本**: 1.0.0
- **描述**: 项目文档管理与代码迭代智能工具，提供自动文档生成、DDD模型管理、记忆压缩和代码风格统一功能

## 项目核心思想

### 交流效率最大化

在软件开发过程中，人与大模型之间的交流存在显著的效率瓶颈：
- **传统打字交流**：速度约为 1-2 比特/秒，信息传递缓慢
- **文档驱动交流**：通过结构化文档传递信息，效率远高于直接对话

### 核心价值

MemDDC 的核心思想是**提高交流效率，减少大模型和人的低速沟通**。通过以下方式实现：

1. **全量代码预扫描**：首次使用时，自动对项目全量代码进行深度扫描，生成详尽的软件工程文档
2. **详细文档生成**：将项目结构、业务逻辑、架构设计、API接口、数据库设计等信息自动转化为结构化文档
3. **DDD领域模型**：建立清晰的领域边界和业务契约，减少沟通歧义，确保代码修改符合领域设计
4. **智能记忆压缩**：将生成的详尽文档进行智能压缩，保留核心信息，减少冗余内容，生成高效的记忆快照
5. **代码风格统一**：通过自动化工具确保代码风格一致性，减少因风格差异导致的沟通成本

### 工作原理的优势

传统的 AI 编码方式是：
- 从一个点开始，然后通过上下文搜索代码
- 频繁的上下文检索，导致编码速度慢，token 消耗大
- 对项目整体理解不足，容易产生不符合架构约束的代码

MemDDC 的优势：
- **预扫描全量代码**：首次使用时对整个项目进行全量扫描，生成详尽的软件工程文档
- **智能压缩优化**：将详尽文档压缩为核心信息，生成记忆快照，减少存储和传输成本
- **快速记忆加载**：后续编码时直接加载压缩的记忆快照，无需重复扫描代码
- **减少上下文检索**：通过记忆快照提供项目整体上下文，减少频繁的代码检索
- **节省 token 消耗**：压缩后的记忆快照体积小，减少上下文注入的 token 消耗
- **编码又快又准**：AI 能够快速理解项目架构和约束，生成符合设计规范的代码

### 工作原理

1. **初始化阶段**：扫描项目并生成完整文档、DDD模型和记忆快照
2. **迭代阶段**：自动加载现有资料作为上下文约束，确保代码修改符合历史架构决策
3. **闭环阶段**：自动同步更新文档和模型，保持所有资料最新且轻量化

## 如何使用

### 手动触发

通过以下关键词触发 MemDDC Skill：
- `MemDDC`
- `加载记忆约束修改`
- `按DDD契约迭代更新`

### 自动调用设置

#### 编辑器自动调用配置

1. **VS Code 配置**：
   - 安装 TRAE 插件
   - 在 `settings.json` 中添加自动触发配置：

   ```json
   {
     "trae.autoInvokeSkills": [
       {
         "skillName": "MemDDC",
         "triggers": [
           "onFileSave",
           "onCodeEdit",
           "onProjectOpen"
         ],
         "conditions": {
           "fileExtensions": [".js", ".ts", ".jsx", ".tsx", ".java", ".py"],
           "projectTypes": ["node", "react", "angular", "vue", "java", "python"]
         }
       }
     ]
   }
   ```

2. **JetBrains 系列 IDE 配置**：
   - 安装 TRAE 插件
   - 在插件设置中启用自动触发：
     - 触发时机：文件保存、代码编辑、项目打开
     - 适用文件类型：所有源代码文件

3. **Sublime Text 配置**：
   - 安装 TRAE 插件
   - 在插件配置文件中添加：

   ```json
   {
     "auto_invoke": true,
     "skills": [
       {
         "name": "MemDDC",
         "triggers": ["on_save", "on_modify", "on_load"]
       }
     ]
   }
   ```

#### 项目级自动调用

在项目根目录创建 `.trae/config.json` 文件：

```json
{
  "autoInvoke": true,
  "skills": [
    {
      "name": "MemDDC",
      "triggers": [
        "onFileCreate",
        "onFileDelete",
        "onFileRename",
        "onBranchChange",
        "onMerge"
      ],
      "options": {
        "scanDepth": 5,
        "compressionLevel": 7,
        "autoUpdateDocs": true,
        "autoEnforceCodeStyle": true
      }
    }
  ]
}
```

## 目录结构

```
/
├── docs/              # 全量工程原始文档 & AI轻量化压缩文档
│   └── code-style.md  # 代码风格指南文档
├── ddd-model.md       # 项目完整DDD领域模型定义
└── mem-snapshot.json  # 全局压缩长记忆快照文件
```

## 技术特点

- **智能文档生成**：自动扫描项目并生成结构化文档
- **DDD领域建模**：建立清晰的领域边界和业务契约
- **长记忆管理**：压缩存储项目历史决策和约束规则
- **代码风格统一**：自动化代码风格检查和修复
- **上下文注入**：确保大模型严格遵循历史架构决策
- **闭环更新**：自动同步更新文档和模型，保持一致性

## 适用场景

- **复杂架构迭代**：确保架构决策的连续性和一致性
- **遗留系统重构**：快速理解现有系统并进行有序重构
- **长期多人维护**：保持代码风格和架构设计的一致性
- **跨团队协作**：通过结构化文档减少沟通成本

## 性能优化

- **增量扫描**：只处理变更的文件，减少扫描时间
- **并行处理**：同时处理多个文档生成任务
- **智能缓存**：缓存中间结果，提高重复操作效率
- **压缩算法**：使用高效压缩算法，减少存储和传输成本

## 故障排除

### 常见问题

1. **初始化失败**
   - 检查项目目录权限
   - 确保项目中包含足够的源码文件
   - 查看日志文件了解具体错误

2. **记忆加载失败**
   - 检查记忆快照文件是否存在
   - 验证文件格式是否正确
   - 尝试重新初始化项目

3. **文档更新失败**
   - 检查文档目录权限
   - 确保磁盘空间充足
   - 查看日志文件了解具体错误

4. **代码风格检查失败**
   - 检查代码风格配置文件是否正确
   - 确保代码风格检查器有执行权限
   - 查看日志文件了解具体错误

## 版本历史

- **v1.0.0** - 初始版本
  - 实现项目扫描和文档生成
  - 支持DDD模型构建
  - 提供记忆压缩和管理功能
  - 实现代码风格统一功能
  - 支持自动调用配置

## 测试结果

### 测试对象

选择了 Express.js 开源项目作为测试对象，这是一个流行的 Node.js web 框架，具有典型的项目结构和代码组织方式。

### 测试过程

1. **不使用 MemDDC 技能**：
   - 手动查看 package.json、index.js、lib/express.js 等核心文件
   - 逐步理解项目结构和功能
   - 规划代码修改

2. **使用 MemDDC 技能**：
   - 执行 MemDDC 初始化流程
   - 自动扫描项目结构和代码
   - 生成详细的软件工程文档
   - 构建完整的 DDD 领域模型
   - 生成压缩的记忆快照

### 测试结果比较

| 指标 | 不使用 MemDDC | 使用 MemDDC |
|------|----------------|--------------|
| 项目文件数量 | 242 | 242 |
| 项目目录数量 | 84 | 84 |
| 记忆快照大小 | N/A | 577 字节 |
| 理解项目时间 | 较长 | 较短 |
| 上下文检索 | 频繁 | 减少 |
| Token 消耗 | 较多 | 较少 |
| 编码速度 | 较慢 | 较快 |
| 编码准确性 | 一般 | 较高 |

### 测试结论

使用 MemDDC 技能的优势：

1. **快速了解项目**：通过全量代码扫描和详细文档生成，快速理解项目整体结构和功能
2. **详细的文档**：生成完整的软件工程文档，包括架构、业务、技术、API等多个方面
3. **完整的 DDD 模型**：构建详细的领域模型，帮助理解业务逻辑和代码结构
4. **智能记忆压缩**：生成压缩的记忆快照，减少上下文检索时间和 token 消耗
5. **编码效率提升**：AI 能够快速理解项目架构和约束，生成符合设计规范的代码
6. **一致性保证**：确保代码、文档、DDD 模型和记忆快照四者一致

### 实际应用建议

1. **首次使用**：在开始一个新项目或接手一个现有项目时，使用 MemDDC 进行初始化，生成详细的文档和领域模型
2. **日常开发**：在进行代码修改时，使用 MemDDC 加载记忆快照，确保代码修改符合项目架构和约束
3. **团队协作**：通过详细的文档和领域模型，减少团队成员之间的沟通成本
4. **代码质量**：通过 DDD 领域模型和代码风格统一，提高代码质量和可维护性

## 总结

MemDDC 技能通过全量代码预扫描、详细文档生成、智能记忆压缩等功能，有效提高了 AI 编码的效率和准确性。它不仅减少了上下文检索的时间和消耗，还确保了代码修改符合项目架构和约束，是一个值得在软件开发过程中使用的智能工具。